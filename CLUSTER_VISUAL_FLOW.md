# 🔍 Cluster Toggle - Visual Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                       USER CLICKS CLUSTER BUTTON                    │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
                    ┌────────────────────────────┐
                    │  setClusteringEnabled(!x)  │
                    └────────────┬───────────────┘
                                 │
                ┌────────────────┴────────────────┐
                │                                 │
                ▼                                 ▼
    ┌───────────────────────┐       ┌───────────────────────┐
    │  Save to localStorage │       │  Trigger useEffect    │
    │  'urbex-clustering-   │       │  (dependency changed) │
    │   enabled' = 'true'   │       └───────────┬───────────┘
    └───────────────────────┘                   │
                                                 ▼
                                ┌────────────────────────────────┐
                                │ ensureSourceAndLayers() called │
                                └────────────┬───────────────────┘
                                             │
                    ┌────────────────────────┼────────────────────────┐
                    │                        │                        │
                    ▼                        ▼                        ▼
        ┌──────────────────┐   ┌──────────────────┐   ┌──────────────────┐
        │  Remove Layers:  │   │  Remove Layers:  │   │  Remove Layers:  │
        │  - spots-circle  │   │  - spots-icon    │   │  - clusters      │
        │  (main pins)     │   │  (details)       │   │  (circles)       │
        └─────────┬────────┘   └─────────┬────────┘   └─────────┬────────┘
                  │                      │                      │
                  └──────────────────────┴──────────────────────┘
                                         │
                                         ▼
                            ┌────────────────────────┐
                            │  Remove Source:        │
                            │  SPOTS_SOURCE_ID       │
                            └───────────┬────────────┘
                                        │
                                        ▼
                ┌───────────────────────────────────────────────┐
                │  Create NEW Source with:                      │
                │  {                                             │
                │    type: "geojson",                            │
                │    data: { features: spotFeatures },           │
                │    cluster: clusteringEnabled  ← KEY!          │
                │    clusterMaxZoom: 14,                         │
                │    clusterRadius: 50                           │
                │  }                                             │
                └───────────────────────┬───────────────────────┘
                                        │
                ┌───────────────────────┴───────────────────────┐
                │                                               │
                ▼                                               ▼
    ┌───────────────────────┐                     ┌───────────────────────┐
    │  IF clusteringEnabled │                     │ ALWAYS add:           │
    │  = true:              │                     │                       │
    │                       │                     │ setupGhostEchoLayers  │
    │  Add cluster layers:  │                     │ (unclustered pins)    │
    │  1. "clusters"        │                     │                       │
    │     (colored circles) │                     │ - spots-circle        │
    │  2. "cluster-count"   │                     │   (▼ or ❤️ pins)      │
    │     (number labels)   │                     │ - spots-icon          │
    │                       │                     │   (architectural      │
    │  Add click handlers:  │                     │    hints)             │
    │  - onClick: zoom in   │                     │                       │
    │  - onHover: pointer   │                     │ Filter: only show     │
    │                       │                     │ if NOT in cluster     │
    └───────────────────────┘                     └───────────────────────┘
                │                                               │
                └───────────────────────┬───────────────────────┘
                                        │
                                        ▼
                            ┌────────────────────────┐
                            │   RENDER COMPLETE      │
                            └────────────────────────┘
                                        │
                ┌───────────────────────┴───────────────────────┐
                │                                               │
                ▼                                               ▼
    ┌───────────────────────┐                     ┌───────────────────────┐
    │  CLUSTERING ON:       │                     │  CLUSTERING OFF:      │
    │                       │                     │                       │
    │  🔵 Cluster circles   │                     │  ▼ Individual pins    │
    │  with counts visible  │                     │  all visible          │
    │                       │                     │                       │
    │  ▼ Individual pins    │                     │  No clusters          │
    │  visible when zoomed  │                     │                       │
    │                       │                     │                       │
    │  Click circle →       │                     │  Click pin →          │
    │  zoom to expand       │                     │  open popup           │
    └───────────────────────┘                     └───────────────────────┘
```

---

## 🎨 Visual States

### State 1: Clustering OFF (Default)

```
Map View:
┌──────────────────────────────────────┐
│                                      │
│     ▼ Montreal   ▼ Laval            │
│                                      │
│   ▼ Longueuil                        │
│                     ▼ Boucherville   │
│                                      │
│  ▼ Châteauguay                       │
│                          ▼ Varennes  │
└──────────────────────────────────────┘

All spots visible as individual pins
```

### State 2: Clustering ON (Zoomed Out)

```
Map View:
┌──────────────────────────────────────┐
│                                      │
│        🔵 15        🟡 22            │
│       (Montreal)   (Laval)           │
│                                      │
│    🔵 8                              │
│  (South Shore)       🔵 5            │
│                    (East)            │
│                                      │
└──────────────────────────────────────┘

Nearby spots grouped into colored circles
Click circle → zoom in to expand
```

### State 3: Clustering ON (Zoomed In)

```
Map View (after clicking Montreal cluster):
┌──────────────────────────────────────┐
│                                      │
│  🔵 4    ▼ Spot A    ▼ Spot B       │
│  (Downtown)                          │
│                                      │
│  ▼ Spot C    🔵 3    ▼ Spot D       │
│            (Mile End)                │
│                                      │
└──────────────────────────────────────┘

Mix of clusters and individual pins
Continue zooming → more pins appear
```

---

## 🔄 Data Flow

```
┌──────────────┐
│   places[]   │  ← Firestore
└──────┬───────┘
       │
       ▼
┌──────────────────┐
│  spotFeatures    │  ← GeoJSON conversion
│  (map format)    │
└──────┬───────────┘
       │
       ▼
┌──────────────────────────────────┐
│  mapInstance.addSource()         │
│  {                               │
│    cluster: clusteringEnabled    │  ← Toggle controls this
│  }                               │
└──────┬───────────────────────────┘
       │
       ├─── IF cluster: true ────┐
       │                         │
       ▼                         ▼
┌──────────────┐      ┌──────────────────┐
│  Mapbox GL   │      │  Unclustered     │
│  calculates  │      │  points          │
│  clusters    │      │  (filter)        │
└──────┬───────┘      └────────┬─────────┘
       │                       │
       ▼                       ▼
┌──────────────┐      ┌──────────────────┐
│  "clusters"  │      │  "spots-circle"  │
│  layer       │      │  "spots-icon"    │
│  (circles)   │      │  layers          │
└──────────────┘      └──────────────────┘
       │                       │
       └───────────┬───────────┘
                   │
                   ▼
            ┌──────────────┐
            │  RENDER MAP  │
            └──────────────┘
```

---

## 🎯 Filter Logic

### Without Clustering

```javascript
source: SPOTS_SOURCE_ID
filter: undefined  // Show all features

Result: All spots visible
```

### With Clustering

```javascript
// Cluster layer
{
  id: "clusters",
  source: SPOTS_SOURCE_ID,
  filter: ["has", "point_count"]  // Only features WITH point_count
}

// Unclustered layer
{
  id: "spots-circle",
  source: SPOTS_SOURCE_ID,
  filter: ["!", ["has", "point_count"]]  // Only features WITHOUT point_count
}

Result: 
- Clustered spots → shown as circles
- Individual spots → shown as pins
```

### Feature Properties

**Cluster Feature** (created by Mapbox):
```json
{
  "type": "Feature",
  "geometry": { "type": "Point", "coordinates": [-73.5, 45.5] },
  "properties": {
    "cluster": true,
    "cluster_id": 123,
    "point_count": 15,           ← Has this property
    "point_count_abbreviated": "15"
  }
}
```

**Unclustered Feature**:
```json
{
  "type": "Feature",
  "geometry": { "type": "Point", "coordinates": [-73.5673, 45.5017] },
  "properties": {
    "id": "abc123",
    "title": "Usine Bellevue",
    "tier": "EPIC",
    "saved": false
    // NO point_count property ← Filter detects this
  }
}
```

---

## ⚡ Performance Impact

```
┌────────────────────────────────────────────────────────┐
│                     Toggle Action                      │
└───────────────────────────┬────────────────────────────┘
                            │
                            ▼
            ┌───────────────────────────┐
            │  Remove Layers: ~5ms      │
            │  Remove Source: ~2ms      │
            │  Create Source: ~10ms     │
            │  Add Layers: ~15ms        │
            └───────────┬───────────────┘
                        │
                        ▼
            ┌───────────────────────────┐
            │  Total: ~30-50ms          │
            │  (imperceptible to user)  │
            └───────────────────────────┘

No camera movement
No map recreation
No flicker
```

---

## 🧩 Layer Stack

### Clustering OFF

```
┌─────────────────────────────┐
│  TOP: spots-icon            │  ← Architectural hints (zoom > 14)
├─────────────────────────────┤
│  spots-circle               │  ← Main pins (▼ or ❤️)
├─────────────────────────────┤
│  Mapbox base style layers   │  ← Streets, buildings, etc.
└─────────────────────────────┘
     BOTTOM
```

### Clustering ON

```
┌─────────────────────────────┐
│  TOP: spots-icon            │  ← Architectural hints (zoom > 14, unclustered only)
├─────────────────────────────┤
│  cluster-count              │  ← Cluster numbers (15, 22, etc.)
├─────────────────────────────┤
│  clusters                   │  ← Cluster circles (🔵 🟡 🌸)
├─────────────────────────────┤
│  spots-circle               │  ← Unclustered pins (▼ or ❤️)
├─────────────────────────────┤
│  Mapbox base style layers   │  ← Streets, buildings, etc.
└─────────────────────────────┘
     BOTTOM
```

---

## ✅ Key Takeaways

1. **Toggle = Source Recreation** (not layer visibility)
2. **Filters = Show Right Features** (cluster vs unclustered)
3. **Click Handlers = Zoom to Expand** (smooth UX)
4. **No Map Rebuild = Fast & Stable** (30-50ms)
5. **LocalStorage = Persist Choice** (remember preference)

**Result**: Reliable, performant, user-friendly clustering toggle ✨
